## 1️⃣0️⃣❎ Rate receiver config

This is the default configuration for the rate receiver module.

To learn more see the [rate receiver](https://doc.log10x.com/run/receive/rate "Filter TenXObjects from output using either local per-node budget sampling or a declarative field-set keyed mute file.") module documentation

