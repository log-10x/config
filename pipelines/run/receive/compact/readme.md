## 1️⃣0️⃣❎ Compact receiver config

This is the default configuration for the compact receiver module.

To learn more see the [compact receiver](https://doc.log10x.com/run/receive/compact "Per-pattern compaction decision via a declarative cap-file. Decides whether each event is emitted via encode() (compact templateHash+vars form, typically 50-80% volume reduction on compaction-capable stacks) or as fullText. GitOps-controlled, the MCP writes per-pattern entries via PR, engine hot-reloads the cap-file without restart.") module documentation

