## 1️⃣0️⃣❎ Rate regulator config

This is the default configuration for the Rate regulator module.

To learn more see the [Rate regulator](https://doc.log10x.com/run/regulate/rate "Filter TenXObjects from output using either local per-node budget sampling or a declarative field-set keyed mute file.") module documentation

