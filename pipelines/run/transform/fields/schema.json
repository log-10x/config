{
  "type" : "object",
  "properties" : {
    "tenx" : {
      "type" : "string"
    },
    "field" : {
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "extract" : {
          "type" : [
            "boolean",
            "string"
          ],
          "markdownDescription" : "Scan TenXTemplate for nested JSON objects or key-value lists\n\nControls whether to scan TenXTemplates for JSON objects or key-value lists (e.g., `{\"price\": 10}` or `price=10`). TenXObjects instances of this template will provide access to these fields as named members (e.g., `this.price`). If a field appears more than once in [text](https://doc.log10x.com/api/js/#TenXBaseObject+text) it is accessible as an array (e.g., `this.price[1]`). (Accepts boolean or string with $= prefix for runtime evaluation) (Default: true)",
          "default" : true
        },
        "nameBreaks" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "KV field name terminator chars\n\nControls which 'text' characters left of a candidate token for being a 'key' in a KV field formation should serve as a terminator for the search.   For example, for an TenXObject whose [text](https://doc.log10x.com/api/js/#TenXBaseObject+text) contains the following entry ',tx_result=OK', the desired key should be 'tx_result', and so the name terminator character should be ',' vs. '_' (in which case the key would have been named 'result'). (Default: , /\\{}.()[])",
          "default" : ", /\\{}.()[]"
        },
        "valueBreaks" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "KV field value terminator chars\n\nControls which characters found to the right of a token whose is a candidate for being a 'value' in a KV field formation should serve  as a terminator for the search.   For example, for an object whose [text](https://doc.log10x.com/api/js/#TenXBaseObject+text) field contains the following entry: 'status=RESULT_SUCCESS' the desired KV value  should be 'RESULT_SUCCESS', and as such the value terminator character  should be ',' vs. '_' (in which case the value would be 'RESULT'). (Default: /\\{}()[])",
          "default" : "/\\{}()[]"
        }
      }
    }
  },
  "additionalProperties" : false
}