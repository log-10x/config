## 1️⃣0️⃣❎ Object storage index output config

This is the default configuration for the Object storage index output module.

To learn more see the [Object storage index output](https://doc.log10x.com/run/input/objectStorage/index "Index files uploaded to a object storage container (e.g. AWS S3 bucket).") module documentation

