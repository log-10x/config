{
  "type" : "object",
  "properties" : {
    "include" : {
      "type" : "string"
    },
    "tenx" : {
      "type" : "string"
    },
    "splunk" : {
      "type" : "array",
      "items" : {
        "type" : "object",
        "additionalProperties" : false,
        "properties" : {
          "name" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Logical name of this Splunk input\n\nSets a logical name (e.g., 'mySplunk') for this input. The [inputName](https://doc.log10x.com/api/js/#TenXBaseObject+inputName) field  returns this value at run time to allow for identifying and operating on instances originating from this input."
          },
          "enabled" : {
            "type" : [
              "boolean",
              "string"
            ],
            "markdownDescription" : "Sets whether this input is enabled (default true)\n\nSets whether to open the input stream. To enable this input only when a [splunkHost](https://doc.log10x.com/run/input/analyzer/splunk/#splunkhost) startup argument value is truthy, use: ``` yaml splunkEnabled: $=TenXEnv.get(\"splunkHost\") ``` To learn more see [TenXEnv.get](https://doc.log10x.com/api/js/#TenXEnv.get). (Accepts boolean or string with $= prefix for runtime evaluation) (Default: true)",
            "default" : true
          },
          "printProgress" : {
            "type" : [
              "boolean",
              "string"
            ],
            "markdownDescription" : "Sets whether this input prints throughput stats to the console\n\nSets whether this input prints throughput stats to the console for testing an integration to a remote endpoint. (Accepts boolean or string with $= prefix for runtime evaluation) (Default: false)",
            "default" : false
          },
          "host" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Splunk host address\n\nSets the Splunk host address to connect to (e.g., `<deployment-name>.splunkcloud.com`)"
          },
          "port" : {
            "type" : [
              "number",
              "string"
            ],
            "markdownDescription" : "Splunk server port\n\nSets the Splunk server port to connect to (e.g., 8089) The port is not needed if the provided [splunkHost](https://doc.log10x.com/run/input/analyzer/splunk/#splunkhost) already encapsulates the port (Accepts number or string with $= prefix for runtime evaluation)"
          },
          "protocol" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Defines the protocol to connect to Splunk\n\nSets the protocol to connect to Splunk with (e.g., https). (Default: https)",
            "default" : "https"
          },
          "username" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Splunk user name\n\nSets the Splunk user name to authenticate with This value is set into the 'username' header of the `/services/search/v2/jobs/` endpoint"
          },
          "password" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Splunk user password\n\nSets the Splunk user password to authenticate with This value is set into the password header of the `/services/search/v2/jobs/` endpoint"
          },
          "verifySSL" : {
            "type" : [
              "boolean",
              "string"
            ],
            "markdownDescription" : "Whether to verify SSL certificates (default true)\n\nSets whether to verify SSL certificates when connecting to Splunk. Set to `false` to allow connections to Splunk instances with self-signed certificates.  **Warning:** Disabling SSL verification is not recommended for production environments.  For example: ``` yaml splunkVerifySSL: false ``` (Accepts boolean or string with $= prefix for runtime evaluation) (Default: true)",
            "default" : true
          },
          "query" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Search query to execute\n\nSets the Splunk search query to execute for this job (Default: search *)",
            "default" : "search *"
          },
          "pageSize" : {
            "type" : [
              "number",
              "string"
            ],
            "markdownDescription" : "Number of events to retrieve with each result page\n\nSets the number of events to retrieve with each result page. **Performance:** Increase to 1000-2000 for high-volume environments to reduce API round-trips. (Accepts number or string with $= prefix for runtime evaluation) (Default: 500)",
            "default" : 500
          },
          "totalBytesLimit" : {
            "type" : [
              "number",
              "string"
            ],
            "markdownDescription" : "Maximum total bytes to read from input before closing\n\nSets the maximum number of bytes a target pipeline input will read into the pipeline. This value limits the volume of events to read from a local/remote source (e.g., log analyzer).  **Performance:** Increase for longer analysis windows (e.g., 200MB for 10min windows).  For example: ``` yaml splunkTotalBytesLimit: $=parseBytes(\"1GB\") ``` (Accepts number or string with $= prefix for runtime evaluation)"
          },
          "totalEventsLimit" : {
            "type" : [
              "number",
              "string"
            ],
            "markdownDescription" : "Maximum total events to read from input before closing\n\nSets the maximum number of events a target pipeline input will read into the pipeline. This value limits the volume of events to read from a local/remote source (e.g., log analyzer).  **Performance:** Adjust based on memory and processing capacity. Each event consumes memory during processing. (Accepts number or string with $= prefix for runtime evaluation) (Default: 10000)",
            "default" : 10000
          },
          "totalDuration" : {
            "type" : [
              "string",
              "null"
            ],
            "markdownDescription" : "Maximum duration to keep input open before closing\n\nSets the maximum duration a target pipeline input will remain open. When reached, the input will close and no more data will be read.  **Performance:** Match to your job scheduling interval (e.g., if running every 10min, set to 10min).  For example: ``` yaml splunkTotalDuration: $=parseDuration(\"10min\") ``` (Default: 5min)",
            "default" : "5min"
          },
          "queryInterval" : {
            "type" : [
              "number",
              "string"
            ],
            "markdownDescription" : "Query interval (in milliseconds) for checking new data from remote source\n\nSets the interval between queries to the remote Splunk API. This controls how frequently the input polls for new log data.  **Performance:** Increase for rate-limited APIs; decrease for real-time needs.  For example: ``` yaml splunkQueryInterval: $=parseDuration(\"5s\") ``` (Accepts number or string with $= prefix for runtime evaluation) (Default: 2000)",
            "default" : 2000
          }
        },
        "required" : [
          "host",
          "query"
        ]
      }
    }
  },
  "additionalProperties" : false
}