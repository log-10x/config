{
  "type" : "object",
  "properties" : {
    "include" : {
      "type" : "array",
      "items" : {
        "type" : "string"
      }
    },
    "tenx" : {
      "type" : "string"
    },
    "filebeat" : {
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "encodeObjects" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "Controls whether events written to a Filebeat forwarder are encoded or written in raw form\n\nSets the format in which to serialize TenXObjects back to the Filebeat forwarder. If true, write events using the [encode](https://doc.log10x.com/api/js/#TenXObject+encode) function. If false, write events in their original form as read from the Filebeat forwarder using the [fullText](https://doc.log10x.com/api/js/#TenXBaseObject+fullText) field.  Defaults to the value of `reducerOptimize` when used with the Reducer app. (Default: false)",
          "default" : "false"
        },
        "outputFields" : {
          "type" : [
            "array",
            "null"
          ],
          "markdownDescription" : "List of TenXObject field names to include alongside the main event in output\n\nSpecifies TenXObject field names to emit alongside the main event text when writing back to the Filebeat forwarder. When empty (default), only the main event field is written. Example fields: level, group, symbolMessage. (Default: [])",
          "items" : {
            "type" : "string"
          },
          "default" : [ ]
        },
        "outputEncodeType" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "Output format when outputFields are set. Possible values: [json, delimited]\n\nSpecifies how the combined output (main event field plus outputFields) is encoded when writing back to the Filebeat forwarder. Possible values: - **json**: formats all fields as a JSON object - **delimited**: formats field values separated by the output delimiter Only takes effect when filebeatOutputFields is set. (Default: delimited)",
          "default" : "delimited"
        }
      }
    }
  },
  "additionalProperties" : false
}