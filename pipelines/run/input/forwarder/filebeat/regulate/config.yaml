# yaml-language-server: $schema=https://schema.log10x.com/pipelines/run/input/forwarder/filebeat/regulate/schema.json

# 🔟❎ 'run' Filebeat regulator configuration

# Configure an input that reads events from a Filebeat forwarder and
# an output that writes regulated TenXObjects back to Filebeat.
# When 'regulatorOptimize' is enabled, events are encoded for volume reduction.

# This config file operates in conjunction with the instructions provided in:
# https://doc.log10x.com/run/input/forwarder/filebeat/regulate/#install

# Set the 10x pipeline to 'run' 
tenx: run

# =============================== Dependencies ================================

include:

  - run/modules/input/forwarder/filebeat/regulate/module.yaml

  # Input module to read events from Filebeat into 10x pipeline
  - run/modules/input/forwarder/filebeat/input

  # Output module to write filtered events from 10x pipeline into Filebeat
  - run/modules/input/forwarder/filebeat/output

# ============================== Filebeat Options ==============================

filebeat:

  # 'encodeObjects' controls output encoding. Defaults to the value of 'regulatorOptimize'.
  encodeObjects: $=yield TenXEnv.get("regulatorOptimize", false)

  # 'outputFields' specifies TenXObject field names to emit alongside the main event.
  #  When set, enrichment fields are included in the sidecar output for forwarder post-processing.
  #  Example: outputFields: [level, group, symbolMessage]
  outputFields: []

  # 'outputEncodeType' controls the output format when outputFields is set.
  #  Possible values: 'json' or 'delimited'
  outputEncodeType: delimited
