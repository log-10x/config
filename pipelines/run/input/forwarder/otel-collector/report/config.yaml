# yaml-language-server: $schema=https://schema.log10x.com/pipelines/run/input/forwarder/otel-collector/report/schema.json

# 🔟❎ 'run' OpenTelemetry Collector reporter configuration

# Configure an input that reads events from an OpenTelemetry Collector forwarder.
# To learn more see https://doc.log10x.com/run/input/forwarder/otel-collector/report/

# Set the 10x pipeline to 'run'
tenx: run

# =============================== Dependencies ================================

include:

  # Input module to read events from OpenTelemetry Collector into 10x pipeline
  - run/modules/input/forwarder/otel-collector/input
