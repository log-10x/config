{
  "type" : "object",
  "properties" : {
    "include" : {
      "type" : "array",
      "items" : {
        "type" : "string"
      }
    },
    "tenx" : {
      "type" : "string"
    },
    "fluentd" : {
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "input" : {
          "type" : "object",
          "additionalProperties" : false,
          "properties" : {
            "port" : {
              "type" : [
                "string",
                "number",
                "null"
              ],
              "markdownDescription" : "TCP port to listen on for events from Fluentd's out_forward plugin\n\nTCP port where Log10x listens for events sent by Fluentd's `out_forward` plugin. Match this against the `port` in your Fluentd `<server>` block. (Default: 24224)",
              "default" : "24224"
            },
            "path" : {
              "type" : [
                "string",
                "null"
              ],
              "markdownDescription" : "Unix domain socket path to listen on (Linux/macOS)\n\nWhen set, Log10x listens on a Unix domain socket instead of TCP — useful on Linux/macOS to avoid TCP overhead. Configure Fluentd's `out_forward` with `<transport unix>` + a matching `path`. Ignored on Windows."
            },
            "messageField" : {
              "type" : [
                "string",
                "null"
              ],
              "markdownDescription" : "Name of the JSON field carrying the original log line\n\nName of the field in each Fluentd record that contains the actual log message text. The default (`log`) matches Fluentd's `in_tail` plugin convention for container and file logs. Set to e.g. `message` if your sources emit the log line under a different key. (Default: log)",
              "default" : "log"
            }
          }
        },
        "output" : {
          "type" : "object",
          "additionalProperties" : false,
          "properties" : {
            "host" : {
              "type" : [
                "string",
                "null"
              ],
              "markdownDescription" : "TCP host of the Fluentd in_forward source receiving processed events\n\nHostname or IP where the Fluentd `in_forward` source is listening for processed events from Log10x. Pairs with `fluentdOutputPort`. (Default: 127.0.0.1)",
              "default" : "127.0.0.1"
            },
            "port" : {
              "type" : [
                "string",
                "number",
                "null"
              ],
              "markdownDescription" : "TCP port of the Fluentd in_forward source receiving processed events\n\nTCP port where the Fluentd `in_forward` source is listening for processed events from Log10x. MUST differ from the port Log10x's own Forward input listens on (default `24224`) or the two would collide on the same socket. (Default: 24225)",
              "default" : "24225"
            },
            "path" : {
              "type" : [
                "string",
                "null"
              ],
              "markdownDescription" : "Unix domain socket path of the Fluentd in_forward source (Linux/macOS)\n\nOptional Unix domain socket path for the Fluentd `in_forward` source. When set, takes precedence over `fluentdOutputHost` / `fluentdOutputPort`. Linux/macOS only — ignored on Windows."
            },
            "encodeType" : {
              "type" : [
                "string",
                "null"
              ],
              "markdownDescription" : "Output format for the Forward record. Possible values: [json, delimited]\n\nHow the rendered event field(s) are encoded on the outgoing Forward record. Possible values: - **delimited**: each top-level field of the rendered record becomes   its own Forward record field — preserves the original record's   structure on the wire. - **json**: the whole rendered record is wrapped as one stringified   field. Defaults to `delimited`, which is almost always what you want. (Default: delimited)",
              "default" : "delimited"
            }
          }
        }
      }
    }
  },
  "additionalProperties" : true
}