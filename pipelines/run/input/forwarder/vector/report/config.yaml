# yaml-language-server: $schema=https://schema.log10x.com/pipelines/run/input/forwarder/vector/report/schema.json

# 🔟❎ 'run' Vector reporter configuration

# Configure an input that reads events from a Vector forwarder.
# To learn more see https://doc.log10x.com/run/input/forwarder/vector/report/

# Set the 10x pipeline to 'run'
tenx: run

# =============================== Dependencies ================================

include:

  # Input module to read events from Vector into the 10x pipeline
  - run/modules/input/forwarder/vector/input
