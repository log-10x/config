## 1️⃣0️⃣❎ Forward protocol input config

This is the default configuration for the Forward protocol input module.

To learn more see the [Forward protocol input](https://doc.log10x.com/run/input/forward "Read events via the Fluent Forward protocol on a Unix domain socket or TCP port") module documentation

