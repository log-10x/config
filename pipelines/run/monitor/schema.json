{
  "type" : "object",
  "properties" : {
    "tenx" : {
      "type" : "string"
    },
    "monitor" : {
      "type" : "object",
      "additionalProperties" : false,
      "properties" : {
        "pollInterval" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "Interval to information to log/ publish counters\n\nSets the frequency to log debug information for pipeline units specified by 'monitorUnits' to log/time series backend. (Default: 1s)",
          "default" : "1s"
        },
        "units" : {
          "type" : [
            "array",
            "null"
          ],
          "markdownDescription" : "Pipeline unit names to log and emit debug metrics\n\nSpecifies which 10x pipeline units will log and emit debug metrics while executing. Each pipeline prints the names of its configured units upon startup on the console and log file.  To emit information for all pipeline units, specify '*'.",
          "items" : {
            "type" : "string"
          }
        },
        "metricPattern" : {
          "type" : [
            "string",
            "null"
          ],
          "markdownDescription" : "Regexp pattern to match against counters to publish as metrics\n\nDefines a pattern to match against counters debug produced by units specified by [monitorUnits](https://doc.log10x.com/shared/monitor/#monitorunits). Each counter provides a name in the following format: `<unit name>_<key0...KeyN>` (e.g., input_events_0).  The pipeline will emit counters matching the pattern, [tagging each counter](https://www.baeldung.com/micrometer#1-tags)  with information about the reporting unit instance (e.g., the destination for a 'run' pipeline output)."
        }
      }
    }
  },
  "additionalProperties" : false
}