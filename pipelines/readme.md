## 1️⃣0️⃣❎ Pipeline Bootstrap config

This is the default configuration for the Pipeline Bootstrap unit.

To learn more see the [Pipeline Bootstrap](https://doc.log10x.com/run/bootstrap "Authenticate the log10x account and launch a target pipeline") unit documentation

