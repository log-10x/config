## 1️⃣0️⃣❎ Compile output printer config

This is the default configuration for the Compile output printer unit.

To learn more see the [Compile output printer](https://doc.log10x.com/compile/console "Print 'compile' results to console") unit documentation

