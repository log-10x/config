## 1️⃣0️⃣❎ Docker image config

This is the default configuration for the Docker image module.

To learn more see the [Docker image](https://doc.log10x.com/compile/pull/docker "Pull Docker image files to scan for symbol content") module documentation

