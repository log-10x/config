## 1️⃣0️⃣❎ Helm chart config

This is the default configuration for the Helm chart module.

To learn more see the [Helm chart](https://doc.log10x.com/compile/pull/helm "Pull GitHub repositories and Docker images to scan for symbol content from Helm charts") module documentation

