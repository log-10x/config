## 1️⃣0️⃣❎ Regex pattern scanner config

This is the default configuration for the Regex pattern scanner module.

To learn more see the [Regex pattern scanner](https://doc.log10x.com/compile/scanner/pattern "Extract symbol values from a files using regular expressions") module documentation

