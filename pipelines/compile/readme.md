## 1️⃣0️⃣❎ Compiler config

This is the compile configuration for the Compiler app.

To learn more see the [Compiler](https://doc.log10x.com/apps/compiler "Invoke the compile pipeline to generate symbol libraries") app documentation

